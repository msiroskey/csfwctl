# CLI reference

Per-command documentation for `csfwctl`. The CLI surface is defined in
`csfwctl-project-plan.md` section 4; subcommands are stubbed in Phase 0
and filled in as later phases land.

Phase 0 placeholder.
